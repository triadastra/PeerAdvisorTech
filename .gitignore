# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets (keep the template)
.env
.env.*
!.env.example

# Backend runtime data + generated secret (never commit)
server/.data

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local tooling, archives, and generated/binary artifacts (keep out of the repo)
.claude/
*.zip
*.docx
super_relay_contour_map*.html
